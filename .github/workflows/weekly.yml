name: weekly-scrape

on:
  schedule:
    - cron: "0 9 * * 1"  # Mondays 09:00 UTC
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: scrape-weekly
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run structured-source scrapers
        run: |
          set -e
          python -m scrapers.sec_edgar
          python -m scrapers.wikipedia

      - name: Run IR scrapers (soft-fail)
        continue-on-error: true
        run: |
          python -m scrapers.nintendo_ir
          python -m scrapers.sony_ir

      - name: Normalize + build SQLite
        run: python -m scrapers.normalize

      - name: Validate schema + canary
        run: pytest -q

      - name: Commit changes (if any)
        run: |
          git config user.name "console-tally-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No data changes."
          else
            git commit -m "chore(data): weekly refresh $(date -u +%Y-%m-%d)"
            for attempt in 1 2 3; do
              git pull --rebase origin main && git push && break
              echo "push attempt $attempt failed; retrying"
              sleep 3
            done
          fi
